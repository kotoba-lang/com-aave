;; Generated by scripts/bb-to-nbb-scaffold.cljs (ADR-2607173000).
;; Classpath for nbb. Prefer `kbb --backend sci -m …` / package.json scripts over bb.edn.
;;
;; :deps is here because nbb reads THIS file and never deps.edn (ADR-2609093000).
;; The coordinate and sha are copied from deps.edn, not chosen: that is the
;; explicit floor this repository pins so it does not ride whatever sha a
;; sibling happens to name. Both files must move together.
;;
;; Measured 2026-09-10: without this, `kbb --backend sci run-tests.cljk` dies with
;; "Could not find namespace: kotoba.lang.text" while `kbb -M:test` stays
;; green, because the JVM suite reads deps.edn and never loads the .cljs.
{:paths ["src" "test"]
 :deps {io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}}}
